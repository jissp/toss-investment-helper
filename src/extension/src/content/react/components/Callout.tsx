import React from 'react';

interface Props {
    children: React.ReactNode;
}

export const Callout: React.FC<Props> = ({ children }: Props) => {
    return <div className={'_1n4jthd0'}>{children}</div>;
};
